name: Setup Investment Desk Python
description: Reusable Python and dependency setup for every desk workflow
runs:
  using: composite
  steps:
    - uses: actions/setup-python@v6
      with:
        python-version: '3.12'
        cache: pip
    - shell: bash
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
